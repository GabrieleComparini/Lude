    # Dependency directories
    node_modules/
    **/node_modules/

    # Build output
    /dist
    /build
    *.out

    # Environment variables files
    .env
    *.env
    .env.*
    !/.env.example # Se hai un file di esempio, puoi escluderlo dall'ignore

    # Log files
    *.log
    logs/
    npm-debug.log*
    yarn-debug.log*
    yarn-error.log*

    # OS generated files
    .DS_Store
    .DS_Store?
    ._*
    .Spotlight-V100
    .Trashes
    ehthumbs.db
    Thumbs.db

    # Editor directories and files
    .idea/
    .vscode/
    *.suo
    *.ntvs*
    *.njsproj
    *.sln
    *.sw?

    # Firebase/GCP Service Account Keys (IMPORTANT!)
    serviceAccountKey.json
    *.json

    # Expo specific
    apps/mobile/.expo/
    apps/mobile/web-build/
    apps/mobile/.idea/

    # Next.js specific
    apps/web/.next/
    apps/web/out/
    # .env.local is usually ignored by default by Next.js, but good to have
    apps/web/.env.local

    # Render specific (if any configuration files are stored)

    # Other
    *.gz
    *.zip
node_modules
