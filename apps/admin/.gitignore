    # Dependency directories
    node_modules
    **/node_modules/
    **/node_modules/**
    /node_modules/

    # Build output
    /dist
    /admin
    /build
    *.out

    # Environment variables files
    .env
    *.env
    .env.*
    !/.env.example # Se hai un file di esempio, puoi escluderlo dall'ignore

    # Log files
    *.log
    logs/
    npm-debug.log*
    yarn-debug.log*
    yarn-error.log*

    # OS generated files
    .DS_Store
    .DS_Store?
    ._*
    .Spotlight-V100
    .Trashes
    ehthumbs.db
    Thumbs.db

    # Editor directories and files
    .idea/
    .vscode/
    *.suo
    *.ntvs*
    *.njsproj
    *.sln
    *.sw?

    # Firebase/GCP Service Account Keys (IMPORTANT!)
    serviceAccountKey.json
    *.json

    # Expo specific
    apps/mobile/.expo/
    apps/mobile/web-build/
    apps/mobile/.idea/


    # Render specific (if any configuration files are stored)

    # Other
    *.gz
    *.zip